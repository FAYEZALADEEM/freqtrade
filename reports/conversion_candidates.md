# تقرير مرشحي التحويل - Pine Script إلى Freqtrade

**التاريخ:** 2026-06-19  
**المصدر:** `reports/pine_index.csv` (113 ملف Pine)  
**الغرض:** تصنيف الاستراتيجيات واختيار أفضل المرشحين لبدء التحويل (المرحلة 5)

## تصنيف الاستراتيجيات

اعتمدنا على درجة القابلية للتحويل المحسوبة في الفهرس (base 80 + تعديلات حسب وجود request.security، pivots، manual SR، entry/exit...):

- **A - قابلة للتحويل مباشرة**: 54 ملف (score ≥ 80)
- **B - قابلة بعد استبدال عناصر يدوية (مثل support/resistance)**: 44 ملف (score 60-79)
- **C - تحتاج إعادة بناء**: 12 ملف (score 40-59)
- **D - غير مناسبة**: 3 ملفات (score < 40)

### ملاحظات عامة على التصنيف
- معظم الملفات هي `strategy` (110 من 113).
- 17 ملف تستخدم `request.security` (تعقيد إضافي في التحويل).
- 55 ملف تستخدم `pivothigh`/`pivotlow` (جيدة للتحويل إلى دعم/مقاومة آلية).
- 44 ملف تحتوي على إشارات دعم/مقاومة يدوية محتملة (تحتاج استبدال بـ Pivot + ATR tolerance).
- لا يوجد ملفات تحتوي على lookahead واضح في التحقق السابق (PINE_V6_VALIDATION_REPORT).

## أفضل 3 استراتيجيات للبدء

### 1. `strategy_01_crt_thick_candle_break.pine` (Score: 90 - فئة A)
**سبب الاختيار:**
- أعلى درجة (90).
- استراتيجية كاملة (ليس مؤشر فقط).
- تستخدم pivots مؤكدة (pivothigh/pivotlow) بدون request.security.
- لا تحتوي على دعم/مقاومة يدوية معقدة.
- قواعد بسيطة نسبيًا تعتمد على كسر شمعة سميكة بعد تأكيد.
- مناسبة لـ 15m (intraday).

**مخاطر التحويل:**
- يجب التأكد من منطق "thick candle" (ربما حجم أو جسم كبير + ATR).
- فلاتر الجلسة معطلة افتراضيًا (سهلة).
- تحتاج تنفيذ دقيق لتأكيد الـ pivot بدون lookahead.

**هل تصلح لاحقًا لـ FreqAI؟** نعم. القواعد واضحة يمكن تحويلها إلى features (مثل %candle_body, %pivot_distance).

**هل تحتوي lookahead محتمل؟** منخفض جدًا (حسب التقرير السابق - تستخدم تأكيد bars).

**هل تحتاج دعم/مقاومة آلية؟** لا (تعتمد على pivots بالفعل). يمكن تحسينها بـ ATR tolerance حول الـ pivot.

### 2. `strategy_02_ict_silver_bullet_liquidity_sweep_fvg.pine` (Score: 90 - فئة A)
**سبب الاختيار:**
- Score عالي وفئة A.
- تستخدم مفاهيم ICT/SMC شائعة ومختبرة (Liquidity Sweep + FVG).
- pivots واضحة + FVG معرف آليًا.
- لا request.security في الجدول.
- مناسبة للفوركس (EURUSD) خاصة في أوقات معينة (Silver Bullet).

**مخاطر التحويل:**
- FVG و Liquidity يحتاجان تعريف دقيق في pandas (مثل fair value gap بين شمعات).
- قد تحتوي على منطق جلسات (لكن معطل افتراضيًا).
- تحتاج التحقق من عدم استخدام بيانات مستقبلية في حساب الـ sweep.

**هل تصلح لاحقًا لـ FreqAI؟** نعم بقوة. يمكن إضافة ميزات مثل distance to liquidity, FVG size, volume confirmation كـ %features.

**هل تحتوي lookahead محتمل؟** منخفض (التقرير يؤكد استخدام تأكيد فقط).

**هل تحتاج دعم/مقاومة آلية؟** جزئيًا. Liquidity zones يمكن معاملتها كـ dynamic SR بـ pivots + ATR.

### 3. `strategy_03_smc_liquidity_grab_imbalance_ob.pine` (Score: 90 - فئة A)
**سبب الاختيار:**
- Score 90، استراتيجية كاملة.
- تستخدم Order Block + Imbalance + Liquidity Grab (مفاهيم قوية في SMC).
- pivots مؤكدة.
- لا يوجد request.security.
- بسيطة نسبيًا في الهيكل.

**مخاطر التحويل:**
- Order Block يحتاج تعريفًا واضحًا (آخر شمعة معاكسة قبل الكسر).
- Imbalance (FVG) يجب حسابه بدقة.
- قد يحتوي على شروط متعددة تحتاج enter_tag مختلفة.

**هل تصلح لاحقًا لـ FreqAI؟** ممتازة. Order blocks و imbalances يمكن أن تكون features قوية.

**هل تحتوي lookahead محتمل؟** منخفض.

**هل تحتاج دعم/مقاومة آلية؟** يمكن تحسينها بـ auto SR حول الـ Order Blocks باستخدام Pivot + ATR tolerance.

## ملخص التوصيات

- **ابدأ بهذه الثلاث** لأنها:
  - فئة A مباشرة.
  - تعتمد على pivots (سهلة التحويل إلى آلية).
  - لا تعتمد على request.security.
  - لها إمكانية جيدة للتطوير لـ FreqAI لاحقًا.
  - مناسبة لزوج EUR/USDT وفريم 15m.

- **الخطوات التالية الموصى بها** (بعد الموافقة):
  1. تحليل مفصل لكل واحدة (المرحلة 6) باستخدام `tools/analyze_pine_strategy.py`.
  2. خطة تحويل مفصلة للاستراتيجية الأولى.
  3. تنفيذ التحويل في `user_data/strategies/` (ملف جديد فقط).

- **ملاحظة هامة**: كل الاستراتيجيات الـ 54 في الفئة A مرشحة جيدة. هذه الثلاثة اخترناها لأنها:
  - بسيطة نسبيًا في البداية.
  - تحتوي على pivots واضحة (تطابق توصيات التحويل في التقرير).
  - من نوع SMC/ICT الذي يعمل جيدًا على الفوركس.

---

**لا يوجد أي تحويل تم حتى الآن.**  
هذا التقرير للتصنيف فقط. سننتظر موافقتك قبل البدء في تحليل أو تحويل أي استراتيجية محددة.

**المصادر:**
- reports/pine_index.csv
- reports/pine_index.md
- PINE_V6_VALIDATION_REPORT.md (سابقًا)
